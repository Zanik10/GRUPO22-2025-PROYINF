{
  "name": "prestamos-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "echo \"Ejecuta client y server por separado\"",
    "build": "echo \"Build manual en apps/client y apps/server\"",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "cross-env": "^10.0.0"
  }
}
